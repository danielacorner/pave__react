# language setting
language: node_js
node_js:
  - 10

build:
  ci:
    - npm install
    - npm test
  on_success:
    - npx codecov
    - npm run deploy
integrations:
  notifications:
    - integrationName: email
      type: email
      recipients:
        - danielcorner7@gmail.com
      sendConsoleLogs: true
      sendFailingSnippet: true
